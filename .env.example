# Copy to .env (ignored by Git) and adjust. Local development values only — never real secrets.
POSTGRES_DB=fulfillmenthub
POSTGRES_USER=fh
POSTGRES_PASSWORD=change-me-local-only
POSTGRES_PORT=5432
ASPIRE_DASHBOARD_PORT=18888
OTLP_GRPC_PORT=4317
LOCALSTACK_PORT=4566
